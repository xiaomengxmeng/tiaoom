收到。既然要 **“6个都要，并且联动更多”** ，我直接将《赛博斗鱼油》现有的 **12个武器槽位全部替换**为来自《某种超能力》角色设定的超能力武器，并额外提供 **3个“全局被动/场地机制”** （作为联动的彩蛋），形成一个完整的 **“某种超能力 x 赛博斗鱼油”联动版本**。

以下设计严格遵循：
- **IWeapon 接口**（onTick / onHitTarget / onHitByAttacker / burst）
- **WeaponRangeConfig** 数值字段
- **VisualEvent 协议**（TRIGGER → UPDATE → REMOVE）
- **5层视觉体系**（L1本体 / L2投射物 / L3场地 / L4界面 / L5全屏）

---

## 🟣 流派一：侵略者（Aggressor）—— 3把

### 武器 1：达达主义（Da-Da-ism）— *Pin*
> **机制**：浮游念力弹自动索敌。

| 项目 | 内容 |
|------|------|
| 形态 | L2 投射物（3颗透明球体绕身公转） |
| 难度 | ⭐⭐ |
| 颜色 | 品红 #FF00FF |

**常驻特性**  
每次球体互撞，在自身周围生成 1 颗 **念力弹**（半径 15px，公转半径 50px，上限 3 颗）。当任一对手进入自身 150px 范围内，所有念力弹自动脱离轨道，以 400px/s 速度追踪最近对手，每颗命中造成 **3 点伤害**，命中后销毁（需重新积攒）。

**爆发方式**：念力弹在 **1.2 秒内全部射空**（即弹匣清空速度）时触发。  

**爆发效果 - 哑弹狂欢**  
立即补满 3 颗念力弹，且接下来 **4 秒内**每次互撞生成 **2 颗**念力弹（上限仍为 3 颗，但射出频率翻倍）。每颗伤害提升至 **5 点**。

**WeaponRangeConfig**
```typescript
[WeaponId.DADAISM]: {
  damage: 3,
  burstDamage: 5,
  maxEnergy: 3,        // 弹药上限
  damageRadius: 150,   // 索敌半径
  visualRadius: 50,    // 公转半径
  projectile: { speed: 400, maxBounces: 0, maxLifetimeSec: 2, hitRadius: 15 },
  field: { maxCount: 3, durationSec: 4, radius: 15 },
}
```

**视觉事件**：`DADA_BALL_SPAWN` / `DADA_BALL_FIRE` / `DADA_BURST`（L5全屏闪白+品红弹幕轨迹线）。

---

### 武器 2：光学斩击（Optical Slash）— *Liya*
> **机制**：穿透墙壁的法线斩击。

| 项目 | 内容 |
|------|------|
| 形态 | L2 投射物（透明刃光） |
| 难度 | ⭐⭐⭐ |
| 颜色 | 品红 #FF00FF |

**常驻特性**  
每次球体互撞时，沿碰撞法线方向生成一道 **笔直斩击**（长 100px，宽 4px），无视墙壁阻挡，对路径上所有对手造成 **5 点穿透伤害**（同一道斩击对每个对手只生效 1 次）。每命中 1 个对手，自身移速 +5%（持续 2 秒，最多叠加 20%）。

**爆发方式**：斩击累计命中 **6 次**后触发。

**爆发效果 - 无限剑制**  
下一次碰撞时，在自身位置生成 **6 道扇形斩击**（每 60° 一道，长度 150px），每道伤害提升至 **10 点**。全屏出现十字准星锁定特效。

**WeaponRangeConfig**
```typescript
[WeaponId.OPTICAL_SLASH]: {
  damage: 5,
  burstDamage: 10,
  maxEnergy: 6,
  damageRadius: 100,
  visualRadius: 150,
  visualDurationMs: 800,
  projectile: { speed: 0, maxBounces: 0, maxLifetimeSec: 0.8, hitRadius: 4 },
}
```

**视觉事件**：`SLASH_TRIGGER`（直线）/ `SLASH_BURST`（扇形，L5屏幕边缘撕裂暗角）。

---

### 武器 3：情绪天气（Emotional Weather）— *Carzeye*
> **机制**：碰撞引发局部雷暴/冰雹。

| 项目 | 内容 |
|------|------|
| 形态 | L3 场地印记（乌云/冰晶） |
| 难度 | ⭐⭐ |
| 颜色 | 品红 #FF00FF（闪电偏白） |

**常驻特性**  
每次球体互撞，在碰撞点 **2 秒后**降下一道 **落雷**（半径 40px 圆形判定），对范围内的对手造成 **6 点伤害**并附带 0.3 秒短暂硬直。落雷有 1.5 秒内置 CD。

**爆发方式**：落雷命中 **5 次**后触发。

**爆发效果 - 极端气候**  
自身周围 200px 内持续降下 **冰雹**（持续 4 秒，每 0.5 秒一次，半径 30px），每颗冰雹造成 **4 点伤害** + 移速 -20%（持续 1 秒）。同时全场画面出现暴风雪滤镜。

**WeaponRangeConfig**
```typescript
[WeaponId.EMOTIONAL_WEATHER]: {
  damage: 6,
  burstDamage: 4,
  maxEnergy: 5,
  damageRadius: 40,
  aoeMaxRadius: 200,
  visualDurationMs: 4000,
  field: { maxCount: 1, durationSec: 4, radius: 30, slowPercent: 20 },
}
```

---

## 🔵 流派二：控制者（Controller）—— 3把

### 武器 4：流体操控（Fluid Mastery）— *KE*
> **机制**：水流尾迹 + 中心水龙卷。

| 项目 | 内容 |
|------|------|
| 形态 | L3 场地印记（动态蓝色水流） |
| 难度 | ⭐⭐ |
| 颜色 | 电蓝 #00BFFF |

**常驻特性**  
球体移动时在身后留下 **水流尾迹**（持续 3 秒，宽 30px）。对手在水流上移速 -30%，每秒受到 **2 点水压伤害**。自身在水流上移速 +15%。

**爆发方式**：水流覆盖地图 **15% 面积**时触发（每 10 秒检测一次）。

**爆发效果 - 水龙卷**  
地图中心生成持续 **4 秒的水龙卷**（半径 120px），强力吸引半径 200px 内的所有对手向中心聚拢，拉到中心造成 **15 点伤害** + 眩晕 1 秒。自身不受影响。

**WeaponRangeConfig**
```typescript
[WeaponId.FLUID_MASTERY]: {
  damage: 2,
  burstDamage: 15,
  maxEnergy: 4,
  damageRadius: 30,
  aoeMaxRadius: 120,
  visualDurationMs: 3000,
  field: { maxCount: 1, durationSec: 3, radius: 120, slowPercent: 30 },
  burstDurationSec: 4,
}
```

**视觉事件**：`FLUID_TRAIL`（L3动态波纹）/ `FLUID_BURST`（L5全屏水雾滤镜 + 漩涡扭曲）。

---

### 武器 5：防护力场（Protective Field）— *念*
> **机制**：受击反推 + 绝对领域。

| 项目 | 内容 |
|------|------|
| 形态 | L1 本体（六边形护盾） + L3 场地 |
| 难度 | ⭐ |
| 颜色 | 电蓝 #00BFFF |

**常驻特性**  
自身受到碰撞伤害时，在受击点瞬间爆发 **排斥力场**（半径 60px），将攻击者沿碰撞方向弹飞 60px，并造成 **3 点反伤**。内置 CD 1.5 秒。

**爆发方式**：累计触发排斥力场 **4 次**后触发。

**爆发效果 - 绝对领域**  
自身半径 100px 内生成持续 **3 秒的强大力场**，任何进入该范围的对手会以 300px/s 的速度持续向外推离，每秒受到 **8 点挤压伤害**。力场期间自身免疫所有碰撞伤害。

**WeaponRangeConfig**
```typescript
[WeaponId.PROTECTIVE_FIELD]: {
  damage: 3,
  burstDamage: 8,
  maxEnergy: 4,
  damageRadius: 60,
  aoeMaxRadius: 100,
  burstDurationSec: 3,
  field: { maxCount: 1, durationSec: 3, radius: 100, contactDamage: 8, slowPercent: 0 },
}
```

**视觉事件**：`FIELD_PUSH`（L3蓝色扩散圈）/ `FIELD_DOMAIN`（L1球体护盾充能 + L5地面力场纹理）。

---

### 武器 6：植物操控（Botanical Control）— *沐里*
> **机制**：藤蔓陷阱束缚。

| 项目 | 内容 |
|------|------|
| 形态 | L3 场地印记（绿色藤蔓） |
| 难度 | ⭐⭐⭐ |
| 颜色 | 电蓝 #00BFFF（偏向翠绿） |

**常驻特性**  
每次球体互撞，在碰撞点生长一簇 **荆棘藤蔓**（持续 4 秒，半径 30px）。对手踩到藤蔓时被 **定身 0.5 秒**，并受到 **3 点伤害**。场上最多同时存在 5 簇藤蔓。

**爆发方式**：场上同时存在 **5 簇藤蔓**时触发。

**爆发效果 - 荆棘丛林**  
所有藤蔓瞬间蔓延至半径 60px（持续 3 秒），每秒造成 **6 点伤害**，且定身时间延长至 1 秒。爆发期间新生成的藤蔓直接以扩大形态出现。

**WeaponRangeConfig**
```typescript
[WeaponId.BOTANICAL_CONTROL]: {
  damage: 3,
  burstDamage: 6,
  maxEnergy: 5,
  damageRadius: 30,
  aoeMaxRadius: 60,
  field: { maxCount: 5, durationSec: 4, radius: 30, contactDamage: 3 },
  burstDurationSec: 3,
}
```

---

## 🟢 流派三：工程师（Engineer）—— 3把

### 武器 7：画作实体化（Drawing Manifest）— *白猫*
> **机制**：召唤兽升级体系。

| 项目 | 内容 |
|------|------|
| 形态 | L2 实体投射（兔子） |
| 难度 | ⭐⭐ |
| 颜色 | 酸绿 #39FF14 |

**常驻特性**  
球体自带一只 **Lv1 小兔玩偶**（半径 20px，跟随球体移动）。每次球体互撞，小兔对最近对手造成 **2 点伤害**，并积攒 1 层“灵感墨水”。

**爆发方式**：墨水积攒 **6 层**时触发。

**爆发效果 - 肌肉兔降临**  
小兔巨大化为 **Lv4 肌肉兔**（半径 50px，持续 5 秒），移速翻倍，碰撞对手造成 **12 点伤害** + 击退。肌肉兔存在期间，球体每撞墙一次，肌肉兔向最近对手发动一次冲刺撞击（额外 8 点伤害）。

**WeaponRangeConfig**
```typescript
[WeaponId.DRAWING_MANIFEST]: {
  damage: 2,
  burstDamage: 12,
  maxEnergy: 6,
  damageRadius: 20,
  aoeMaxRadius: 50,
  burstDurationSec: 5,
  projectile: { speed: 200, maxBounces: 0, maxLifetimeSec: 5, hitRadius: 20 },
}
```

**视觉事件**：`INK_STACK`（L4进度条）/ `RABBIT_TRANSFORM`（L2肌肉膨胀动画 + 绿色能量裂纹）。

---

### 武器 8：材料构造（Material Construct）— *波波*
> **机制**：合金方块障碍物 + 要塞合并。

| 项目 | 内容 |
|------|------|
| 形态 | L3 场地印记（酸绿半透明方块） |
| 难度 | ⭐⭐⭐ |
| 颜色 | 酸绿 #39FF14 |

**常驻特性**  
球体碰撞墙壁时，在碰撞位置外侧生成一个 **3×3 方块障碍物**（边长 50px，持续 12 秒）。对手碰撞方块 = 4 点伤害。场上最多 6 个方块。

**爆发方式**：场上同时存在 **6 个方块**时触发。

**爆发效果 - 要塞模式**  
所有方块合并为一堵大型墙壁（横跨地图 40%，持续 5 秒）。墙壁可反弹对手，碰撞墙壁造成 **12 点伤害**。自身可穿过墙壁。

**WeaponRangeConfig**
```typescript
[WeaponId.MATERIAL_CONSTRUCT]: {
  damage: 4,
  burstDamage: 12,
  maxEnergy: 6,
  damageRadius: 50,
  field: { maxCount: 6, durationSec: 12, radius: 50, contactDamage: 4 },
  burstDurationSec: 5,
}
```

---

### 武器 9：祈愿灵（Wish Spirit）— *贝贝*
> **机制**：祈愿光斑回血 + 愿望实现爆发。

| 项目 | 内容 |
|------|------|
| 形态 | L3 场地印记（金色光斑） |
| 难度 | ⭐⭐ |
| 颜色 | 酸绿 #39FF14（偏金） |

**常驻特性**  
球体移动时在路径上留下 **祈愿光斑**（持续 5 秒，半径 20px）。光斑对自身提供每秒 **2 点回血**（踏入光斑即可触发，光斑不消失直到持续结束）。

**爆发方式**：光斑覆盖地图 **20% 面积**时触发。

**爆发效果 - 愿望实现**  
所有光斑瞬间化为光球飞向自身，每颗光球立即恢复 **3 点生命值**（最多 30 点），并给予 **3 秒 30% 移速加成**。

**WeaponRangeConfig**
```typescript
[WeaponId.WISH_SPIRIT]: {
  damage: 0,           // 无直接伤害
  maxEnergy: 4,
  visualRadius: 20,
  field: { maxCount: 20, durationSec: 5, radius: 20 },
  burstDurationSec: 3,
}
```

---

## 🟡 流派四：变奏者（Wildcard）—— 3把

### 武器 10：情绪掌控（Emotion Mastery）— *林澈*
> **机制**：三心境轮盘 + 三实体召唤。

| 项目 | 内容 |
|------|------|
| 形态 | L1 核心光环（颜色动态变化） |
| 难度 | ⭐⭐⭐ |
| 颜色 | 亮黄 #FFD700（三色切换） |

**常驻特性**  
球体每隔 **6 秒**自动在三种“心境”间轮转：
- **愤怒（红）**：碰撞伤害 +30%，但受伤害 +20%。
- **幸福（蓝）**：碰撞命中对手时使其减速 20%（持续 2 秒）。
- **开心（绿）**：每次碰撞回复 **2 点生命值**（满血则转化为临时护盾，上限 10 点）。

**爆发方式**：在 **5 秒内连续切换 3 种心境**（即完整轮转一圈）时触发。

**爆发效果 - 情绪实体化**  
召唤 **愤怒恶魔（红）**、**幸福老者（蓝）**、**开心小孩（绿）** 三个实体围绕自身旋转（半径 80px，持续 4 秒）。任意对手接触实体：
- 红：10 点伤害。
- 蓝：定身 1 秒。
- 绿：自身攻速/移速 +20%（可叠加，上限 40%）。

**WeaponRangeConfig**
```typescript
[WeaponId.EMOTION_MASTERY]: {
  damage: 0,
  burstDamage: 10,
  maxEnergy: 3,        // 心境轮转次数
  damageRadius: 80,
  burstDurationSec: 4,
  field: { maxCount: 3, durationSec: 4, radius: 80, contactDamage: 10, slowPercent: 20 },
}
```

---

### 武器 11：窥探诅咒（Peeping Curse）— *张秋琅*
> **机制**：诅咒层数反伤 + 真实伤害引爆。

| 项目 | 内容 |
|------|------|
| 形态 | L4 数据界面（诅咒层数UI） |
| 难度 | ⭐⭐ |
| 颜色 | 亮黄 #FFD700（暗紫诅咒色） |

**常驻特性**  
任何对自身造成伤害的对手，被施加 1 层 **窥探诅咒**（持续 6 秒，可叠加，最多 5 层）。每层诅咒使对手移速 -5%，且每次攻击自身时有 20% 概率 **丢失**（攻击无效）。

**爆发方式**：任意对手身上诅咒叠满 **4 层**时触发（针对该对手）。

**爆发效果 - 死亡凝视**  
对身上有诅咒的 **所有对手** 造成 **20 点真实伤害**（无视防御），并清除所有诅咒层数。同时全屏出现巨大紫色眼睛虚影。

**WeaponRangeConfig**
```typescript
[WeaponId.PEEPING_CURSE]: {
  damage: 0,
  burstDamage: 20,     // 真实伤害
  maxEnergy: 4,
  damageRadius: 0,
  field: { maxCount: 5, durationSec: 6, slowPercent: 5 },
}
```

---

### 武器 12：时间穿梭（Time Shuttle）— *小奇*
> **机制**：位置回溯 + 静止残影。

| 项目 | 内容 |
|------|------|
| 形态 | L1 本体（残影拖尾）+ L2 投射物 |
| 难度 | ⭐⭐⭐ |
| 颜色 | 亮黄 #FFD700（银色流光） |

**常驻特性**  
球体每 **3 秒**自动记录一次当前位置（最多存储 3 个快照）。当球体碰撞墙壁时，自动闪现回 **最近的快照位置**（CD 2 秒），并产生一次 **时间涟漪**（半径 50px），对涟漪内对手造成 **4 点伤害**。

**爆发方式**：成功闪现（穿越）**3 次**后触发。

**爆发效果 - 时间裂缝**  
在地图随机位置生成 **3 个静止球体残影**（持续 2 秒），敌人碰撞残影受到 **8 点伤害** + 减速 30%（持续 1.5 秒）。自身在爆发期间无视碰撞。

**WeaponRangeConfig**
```typescript
[WeaponId.TIME_SHUTTLE]: {
  damage: 4,
  burstDamage: 8,
  maxEnergy: 3,
  damageRadius: 50,
  visualRadius: 50,
  field: { maxCount: 3, durationSec: 2, radius: 50, slowPercent: 30 },
}
```

---

## 🌐 额外联动：3个全局被动/场地机制（彩蛋）

以下超能力不占用武器槽，作为**地图随机事件**或**全局光环**出现在对局中（每局随机激活 1 个）：

| 超能力 | 全局效果 |
| :--- | :--- |
| **古今观察者（小梦）** | 开局 15 秒，全屏闪白 0.1 秒，**回溯** 2 秒前双方的位置和血量，迷惑对手预判。 |
| **俺寻思之力（薯饼）** | 每 10 秒，弹球速度/方向发生一次 **微小随机扰动**（±5% 速度，±3° 角度），增加不可预测性，视觉效果为全屏马赛克闪动。 |
| **万物亲和（君）** | 双方球体之间增加一根 **淡粉色牵引线**（距离越近线越亮），双方互撞伤害 **-15%**，但移速 **+10%**（促进缠斗，但不鼓励一击必杀）。 |

---

## 📋 完整 WeaponId 枚举（12把）

```typescript
export enum WeaponId {
  // 侵略者
  DADAISM = 'dadaism',
  OPTICAL_SLASH = 'optical_slash',
  EMOTIONAL_WEATHER = 'emotional_weather',
  // 控制者
  FLUID_MASTERY = 'fluid_mastery',
  PROTECTIVE_FIELD = 'protective_field',
  BOTANICAL_CONTROL = 'botanical_control',
  // 工程师
  DRAWING_MANIFEST = 'drawing_manifest',
  MATERIAL_CONSTRUCT = 'material_construct',
  WISH_SPIRIT = 'wish_spirit',
  // 变奏者
  EMOTION_MASTERY = 'emotion_mastery',
  PEEPING_CURSE = 'peeping_curse',
  TIME_SHUTTLE = 'time_shuttle',
}
```



### 🟣 扩展-侵略者（替代方案）

#### 武器 13：心象能束（Mental Energy Beam）— *日暮落（日落）*
> **机制**：碰撞蓄能 → 激光束扫射，反射墙壁增加伤害。

| 项目 | 内容 |
|------|------|
| 形态 | L2 投射物（粗激光束）+ L5 全屏扫描线 |
| 难度 | ⭐⭐⭐ |
| 颜色 | 品红 #FF00FF 渐变亮白 |

**常驻特性**  
每次球体互撞，积攒 1 层 **心象能量**（最多 5 层）。每层使下一次碰撞后释放的 **激光束**（长 150px，宽 8px）伤害 +1。激光束碰到墙壁会**反射 1 次**（继续前进），每次反射后续航距离减半。单次激光束最多命中 2 个对手。

**爆发方式**：心象能量叠满 **5 层**时（下一次互撞自动触发）。

**爆发效果 - 全屏集束**  
释放 **5 道激光束** 呈扇形（每 30° 一道），每道长度 200px，可反射 2 次，单道基础伤害 **12 点**。爆发后能量清空。

**WeaponRangeConfig**
```typescript
[WeaponId.MENTAL_BEAM]: {
  damage: 0,            // 基础伤害由层数决定
  burstDamage: 12,
  maxEnergy: 5,
  damageRadius: 150,
  visualRadius: 8,
  projectile: { speed: 600, maxBounces: 2, maxLifetimeSec: 1.5, hitRadius: 8 },
  burstDurationSec: 2,
}
```

---

### 🔵 扩展-控制者

#### 武器 14：花瓣散华（Flower Petal Scattering）— *虞美人*
> **机制**：碰撞时本体分裂为花瓣（无敌帧）→ 重组造成范围切割。

| 项目 | 内容 |
|------|------|
| 形态 | L2 实体投射（无数漂浮花瓣） |
| 难度 | ⭐⭐⭐ |
| 颜色 | 电蓝 #00BFFF 偏淡粉紫 |

**常驻特性**  
每次球体互撞时，自身化为 **6 片花瓣**（每片半径 12px，向四周扩散 80px，持续 0.6 秒），期间自身**不可被碰撞**（无敌帧）。0.6 秒后花瓣在 **自身当前位置** 重组成球体。重组瞬间对 60px 范围内的所有对手造成 **8 点切割伤害**（无视距离衰减）。

**爆发方式**：累计通过花瓣重组造成伤害 **4 次**后触发。

**爆发效果 - 千本樱**  
下一次碰撞时，自身化为 **18 片花瓣**，扩散半径扩大至 150px，持续 1.2 秒。重组时对 100px 范围内所有对手造成 **15 点伤害**，且每片花瓣飞过的路径留下 **持续 1 秒的切割线**（碰线造成 3 点伤害）。

**WeaponRangeConfig**
```typescript
[WeaponId.PETAL_SCATTER]: {
  damage: 8,
  burstDamage: 15,
  maxEnergy: 4,
  damageRadius: 60,
  aoeMaxRadius: 150,
  visualDurationMs: 1200,
  projectile: { speed: 200, maxBounces: 0, maxLifetimeSec: 0.6, hitRadius: 12 },
}
```

---

### 🟢 扩展-工程师

#### 武器 15：放电猫猫（Discharge Cat）— *小金喵*
> **机制**：召唤闪电猫 → 连锁传导伤害。

| 项目 | 内容 |
|------|------|
| 形态 | L2 实体投射（带电猫形虚影） |
| 难度 | ⭐⭐ |
| 颜色 | 酸绿 #39FF14（闪电为亮白/黄） |

**常驻特性**  
球体周围跟随一只 **放电猫虚影**（半径 15px，始终保持在自身 30px 范围内）。每次球体互撞，放电猫向最近对手发射一道 **电弧**（瞬间命中，范围 120px），造成 **4 点伤害**，并弹射至该对手 80px 内的另一名对手（若存在），造成 **2 点额外伤害**（最多弹射 2 次）。

**爆发方式**：电弧累计弹射总次数达到 **6 次**时触发。

**爆发效果 - 雷霆万钧**  
放电猫实体化（持续 4 秒），移速提升至 2 倍，电弧基础伤害提升至 **8 点**，弹射次数上限提升至 **4 次**，且弹射距离扩大至 120px。爆发期间每次碰撞必定触发一次电弧（无视内置 CD）。

**WeaponRangeConfig**
```typescript
[WeaponId.DISCHARGE_CAT]: {
  damage: 4,
  burstDamage: 8,
  maxEnergy: 6,
  damageRadius: 120,
  visualRadius: 30,
  field: { maxCount: 1, durationSec: 4, radius: 120, contactDamage: 8 },
}
```

## ✅ 下一步落地建议

1. **后端**：按 `WeaponTemplate.ts` 依次实现 12 个武器类，关键逻辑集中在 `onHitTarget`（碰撞）和 `onTick`（持续效果/检测）。
2. **前端**：新建 12 个 `EffectRenderer`（参考 `EffectRendererTemplate.ts`），在 `CyberFishRenderer.ts` 和 `useFishOilBattle.ts` 中注册对应 `VisualEventType`。
3. **数值**：将上述 `WeaponRangeConfig` 填入 `WeaponRangeConfig.ts`，按 `balance-guide.md` 微调（确保伤害/能量比合理）。
4. **UI**：L4 层为每把武器设计专属 HUD（进度条/层数/心境指示器）。

